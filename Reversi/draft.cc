/*
void Cell::printColour(Colour c) {
    if (c == Colour::White) {
        std::cout << " White ";
    } else if (c == Colour::Black) {
        std::cout << " Black ";
    } else {
        std::cout << " NC ";
    }
}


void Cell::printDirection(Direction d) {
    if (d == Direction::N) {
        std::cout << "d is N" << std::endl;
    } else if (d == Direction::S) {
        std::cout << "d is S" << std::endl;
    } else if (d == Direction::W) {
        std::cout << "d is W" << std::endl;
    } else if (d == Direction::E) {
        std::cout << "d is E" << std::endl;
    } else if (d == Direction::NW) {
        std::cout << "d is NW" << std::endl;
    } else if (d == Direction::SW) {
        std::cout << "d is SW" << std::endl;
    } else if (d == Direction::NE) {
        std::cout << "d is NE" << std::endl;
    } else if (d == Direction::SE) {
        std::cout << "d is SE" << std::endl;
    }
}
*/
